<form method="post" action="{{ path('app_backend_showbiz_album_delete', {'id': showbiz_album.id}) }}" onsubmit="return confirm('Voulez-vous vraiment cet album?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ showbiz_album.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
