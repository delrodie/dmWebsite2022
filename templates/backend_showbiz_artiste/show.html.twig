<div class="modal-dialog modal-dialog-centered modal-lg" role="document">
    <div class="modal-content">
        <div class="modal-header">
            <h6 class="modal-title" id="exampleModalLabel4">{{ showbiz_artiste.nom }}</h6>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true"><i data-feather="x"></i></span>
            </button>
        </div>
        <div class="modal-body">
            <p class="mt-3">
                <img src="{{ absolute_url(asset('upload/showbiz/label/artistes/'~ showbiz_artiste.media)) }}" alt="{{ showbiz_artiste.nom }}" class="img-thumbnail" width="300" style="float: left; margin-right: 20px;">
                {{ showbiz_artiste.biographie|raw }}
            </p>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
            {{ include('backend_showbiz_artiste/_delete_form.html.twig') }}
            <a href="{{ path('app_backend_showbiz_artiste_edit', {'id': showbiz_artiste.id}) }}" class="btn btn-primary"> Modifier</a>
        </div>
    </div>
</div>
