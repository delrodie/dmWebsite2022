<form method="post" action="{{ path('app_backend_showbiz_publicite_delete', {'id': showbiz_publicite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ showbiz_publicite.id) }}">
    <button class="btn">Delete</button>
</form>
