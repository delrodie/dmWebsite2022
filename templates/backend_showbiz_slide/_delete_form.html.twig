<form method="post" action="{{ path('app_backend_showbiz_slide_delete', {'id': showbiz_slide.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette image?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ showbiz_slide.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
